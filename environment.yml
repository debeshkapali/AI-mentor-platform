name: mentor-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - setuptools
  - wheel
  - pip:
      - --upgrade pip
      - streamlit
      - grok
      - python-dotenv
